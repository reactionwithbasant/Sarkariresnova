name: SarkariResNova Final Engine
on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'
jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install Libraries
        run: npm install axios rss-parser
      - name: Run AI Generator
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        run: node scripts/generator.js
      - name: Save Results
        run: |
          git config --global user.name "Sarkari Bot"
          git config --global user.email "bot@sarkariresnova.com"
          git add .
          git commit -m "Auto Update: Jobs Added" || echo "No changes"
          git push
